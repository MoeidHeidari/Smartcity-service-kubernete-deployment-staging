import { ProviderSchema, TerraformProviderGenerator } from "@cdktf/provider-generator";
export declare type TerraformResourceBlock = unknown;
export declare type Scope = {
    providerSchema: ProviderSchema;
    providerGenerator: Record<string, TerraformProviderGenerator>;
    constructs: Set<string>;
    variables: Record<string, {
        resource: string;
        variableName: string;
    }>;
};
