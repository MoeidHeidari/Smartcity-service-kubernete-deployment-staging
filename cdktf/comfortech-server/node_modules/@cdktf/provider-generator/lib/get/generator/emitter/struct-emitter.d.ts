import { CodeMaker } from "codemaker";
import { ResourceModel, Struct } from "../models";
import { AttributesEmitter } from "./attributes-emitter";
export declare class StructEmitter {
    private readonly code;
    attributesEmitter: AttributesEmitter;
    constructor(code: CodeMaker);
    emit(resource: ResourceModel): void;
    private warnAboutIdField;
    emitInterface(resource: ResourceModel, struct: Struct, name?: string): void;
    private emitStructs;
    private emitNamespacedStructs;
    private emitClass;
    private emitComplexListClass;
    private emitComplexMapClass;
    private emitInternalValueGetter;
    private emitInternalValueSetter;
    private emitToTerraformFunction;
}
