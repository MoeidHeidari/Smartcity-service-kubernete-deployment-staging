import { Schema } from "./provider-schema";
import { ResourceModel } from "./models";
export declare class ResourceParser {
    private uniqueClassnames;
    private resources;
    parse(provider: string, type: string, schema: Schema, terraformType: string): ResourceModel;
    getClassNameForResource(terraformType: string): string;
}
