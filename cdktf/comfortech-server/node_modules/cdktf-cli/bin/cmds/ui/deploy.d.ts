import React from "react";
import { DeployingResource } from "../../../lib/models/terraform";
import { NestedTerraformOutputs } from "../../../lib/output";
interface DeploySummaryConfig {
    resources: DeployingResource[];
}
export declare const DeploySummary: ({ resources, }: DeploySummaryConfig) => React.ReactElement;
interface DeployConfig {
    outDir: string;
    targetStacks?: string[];
    synthCommand: string;
    autoApprove: boolean;
    onOutputsRetrieved: (outputs: NestedTerraformOutputs) => void;
    outputsPath?: string;
    ignoreMissingStackDependencies?: boolean;
    parallelism?: number;
    refreshOnly?: boolean;
}
export declare const Deploy: ({ outDir, targetStacks, synthCommand, autoApprove, onOutputsRetrieved, outputsPath, ignoreMissingStackDependencies, parallelism, refreshOnly, }: DeployConfig) => React.ReactElement;
export {};
