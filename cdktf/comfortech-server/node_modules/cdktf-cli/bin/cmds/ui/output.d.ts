import React from "react";
import { NestedTerraformOutputs } from "../../../lib/output";
declare type OutputConfig = {
    outDir: string;
    targetStacks?: string[];
    synthCommand: string;
    onOutputsRetrieved: (outputs: NestedTerraformOutputs) => void;
    outputsPath?: string;
};
export declare const Output: ({ outDir, targetStacks, synthCommand, onOutputsRetrieved, outputsPath, }: OutputConfig) => React.ReactElement;
export {};
