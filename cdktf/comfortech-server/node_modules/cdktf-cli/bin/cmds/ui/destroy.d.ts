import React from "react";
interface DestroyConfig {
    outDir: string;
    targetStacks?: string[];
    synthCommand: string;
    autoApprove: boolean;
    ignoreMissingStackDependencies?: boolean;
    parallelism?: number;
}
export declare const Destroy: ({ outDir, targetStacks, synthCommand, autoApprove, ignoreMissingStackDependencies, parallelism, }: DestroyConfig) => React.ReactElement;
export {};
