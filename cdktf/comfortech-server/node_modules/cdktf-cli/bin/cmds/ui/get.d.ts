import React from "react";
import { Language, config } from "@cdktf/provider-generator";
interface GetConfig {
    codeMakerOutput: string;
    language: Language;
    constraints: config.TerraformDependencyConstraint[];
}
export declare const Get: ({ codeMakerOutput, language, constraints, }: GetConfig) => React.ReactElement;
export {};
