import React from "react";
interface WatchConfig {
    targetDir: string;
    targetStacks?: string[];
    parallelism?: number;
    synthCommand: string;
    autoApprove: boolean;
}
export declare const Watch: ({ targetDir, targetStacks, synthCommand, autoApprove, parallelism, }: WatchConfig) => React.ReactElement;
export {};
