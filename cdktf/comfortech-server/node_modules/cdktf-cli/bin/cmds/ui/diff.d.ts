import React from "react";
interface DiffConfig {
    outDir: string;
    targetStack?: string;
    synthCommand: string;
    refreshOnly?: boolean;
}
export declare const Diff: ({ outDir, targetStack, synthCommand, refreshOnly, }: DiffConfig) => React.ReactElement;
export {};
