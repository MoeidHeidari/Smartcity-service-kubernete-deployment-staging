import React from "react";
interface CommonSynthConfig {
    outDir: string;
}
interface SynthConfig extends CommonSynthConfig {
    synthCommand: string;
}
export declare const Synth: ({ outDir, synthCommand, }: SynthConfig) => React.ReactElement;
export {};
