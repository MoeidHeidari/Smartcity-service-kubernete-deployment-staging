export interface Attributes {
    username: string;
    "avatar-url": string;
}
export interface Data {
    id: string;
    type: string;
    attributes: Attributes;
}
export interface Account {
    data: Data;
}
export interface OrganizationData {
    id: string;
    type: string;
    attributes: Record<string, unknown>;
}
export interface Organization {
    data: OrganizationData[];
}
export declare function getAccountDetails(tfeHostname: string, token: string): Promise<Account>;
export declare function createWorkspace(tfeHostname: string, organizationName: string, workspaceName: string, token: string): Promise<void>;
export declare function getOrganizationNames(tfeHostname: string, token: string): Promise<Organization>;
