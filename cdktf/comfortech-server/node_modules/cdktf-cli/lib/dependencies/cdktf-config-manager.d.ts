import { ProviderConstraint } from "./dependency-manager";
/**
 * facilitates adding provider dependencies to cdktf.json
 */
export declare class CdktfConfigManager {
    private config;
    hasProvider(constraint: ProviderConstraint): Promise<boolean>;
    addProvider(constraint: ProviderConstraint): Promise<void>;
}
